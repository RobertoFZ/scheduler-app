# Facebook App Credentials
APP_ID=your_facebook_app_id
APP_SECRET=your_facebook_app_secret
REDIRECT_URI=http://localhost:8000/callback

# Application Settings
SECRET_KEY=your-secure-secret-key-for-sessions

# Database Settings (for Docker setup)
# This is already set in docker-compose.yml, but you can override it here if needed
DATABASE_URL=postgresql://postgres:postgres@db:5432/facebook_scheduler 